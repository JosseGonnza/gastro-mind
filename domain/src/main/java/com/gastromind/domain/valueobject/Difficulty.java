package com.gastromind.domain.valueobject;

public enum Difficulty {
    EASY,       // Recetas básicas (ensaladas, tostadas)
    MEDIUM,     // Elaboraciones estándar (guisos, arroces)
    HARD        // Alta cocina, técnicas avanzadas
}